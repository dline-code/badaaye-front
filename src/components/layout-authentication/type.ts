import { IconType } from "react-icons"

export type LayoutProps = {
  title: String
  description: String
  childrenLink?: String
  ArrowIcon?: IconType
  link?: any
  childrenContainerSelect?: React.ReactNode
}
